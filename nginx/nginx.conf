events {
    worker_connections 1024;
}

http {
    # Rate limiting - הגנה מפני brute force
    # מגביל ל-10 requests לדקה (מתאים גם לטסטים)
    limit_req_zone $binary_remote_addr zone=auth_limit:10m rate=10r/m;
    limit_req_status 429;

    # הגדרות לוג
    log_format auth_log '$remote_addr - $remote_user [$time_local] '
                        '"$request" $status $body_bytes_sent '
                        '"$http_referer" "$http_user_agent"';

    access_log /var/log/nginx/access.log auth_log;
    error_log /var/log/nginx/error.log warn;

    # הגדרות upstream לשירותים
    upstream auth_service {
        server auth-service:5001;
    }

    upstream quiz_service {
        server quiz-app:5002;
    }

    server {
        listen 80;
        server_name localhost;

        # הגדרות כלליות
        client_max_body_size 10M;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;

        # ניתוב לשירות ההתחברות
        location /login {
            proxy_pass http://auth_service;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Cookie $http_cookie;
            # העברת cookies
            proxy_pass_header Set-Cookie;
        }

        location /auth {
            # הגבלת קצב - מקסימום 10 ניסיונות לדקה + burst של 5
            limit_req zone=auth_limit burst=5 nodelay;

            proxy_pass http://auth_service;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Cookie $http_cookie;
            # העברת cookies
            proxy_pass_header Set-Cookie;
        }

        location /verify {
            proxy_pass http://auth_service;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Cookie $http_cookie;
            # העברת cookies
            proxy_pass_header Set-Cookie;
        }

        # ניתוב לאפליקציית החידות
        location /quiz {
            proxy_pass http://quiz_service;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Cookie $http_cookie;
            # העברת cookies
            proxy_pass_header Set-Cookie;
        }

        location /api/ {
            proxy_pass http://quiz_service;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Cookie $http_cookie;
            # העברת cookies
            proxy_pass_header Set-Cookie;
        }

        location /logout {
            proxy_pass http://auth_service;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Cookie $http_cookie;
            # העברת cookies
            proxy_pass_header Set-Cookie;
        }

        # ברירת מחדל - הפניה ל-login
        location / {
            return 301 /login;
        }
    }
}